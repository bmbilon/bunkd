{
  "cli": { "version": ">= 9.0.0" },
  "build": {
    "production": {
      "distribution": "store",
      "installCommand": "cd apps/mobile && npm ci || npm install"
    },
    "preview": {
      "distribution": "internal",
      "installCommand": "cd apps/mobile && npm ci || npm install"
    },
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "installCommand": "cd apps/mobile && npm ci || npm install"
    }
  }
}
